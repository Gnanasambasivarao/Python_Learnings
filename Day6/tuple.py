# tuple: It is similar to list but it is immutable and it can contains duplicates

# Declaration
a=(2,4.5,6+7j,"Samba","siva")

# As we know that tuple is immutable one we add or gie input in tuple we cannot change any any value inside tuple by using method.